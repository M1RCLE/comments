scalar CreationTime

type Comment {
    id: ID!,
    userId: ID!,
    postId: ID!,
    body: String!,
    parentId: ID,
    indentation: Int!,
    creationTime: CreationTime!
    relatedComments: [Comment!],
}

input CommentInput {
    userId: ID!,
    postId: ID!,
    creationTime: CreationTime!,
    body: String!,
}

input SubCommentInput {
    userId: ID!,
    postId: ID!,
    parentId: ID!,
    creationTime: CreationTime!,
    body: String!,
}